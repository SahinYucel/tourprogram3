// Tour işlemleri için yardımcı fonksiyonlar
export const formatTourForSave = (tour, agencyUser) => {
  // ... tour verilerini API formatına dönüştürme mantığı
};

export const formatTourForEdit = (tour, savedCompanies, bolgeler) => {
  // ... tour verilerini düzenleme formatına dönüştürme mantığı
}; 